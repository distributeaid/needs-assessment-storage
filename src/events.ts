export enum events {
	/** Emitted when a user registers */
	user_registered = 'user_registered',
	/** Emitted when an assessment is created */
	assessment_created = 'assessment_created',
	/** Emitted when a correction is created */
	correction_created = 'correction_created',
}
